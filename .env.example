# Example .env file
# Copy this file and rename it to .env, then fill in the appropriate values.

# Server Configuration
PORT=3000                   # The port your application will run on

# Database Configuration
DB_HOST=localhost            # Database host (e.g., localhost or an IP address)
DB_PORT=5432                 # Database port (default for PostgreSQL is 5432)
DB_NAME=example_db           # Replace with your database name
DB_USER=example_user         # Replace with your database username
DB_PASSWORD=example_password # Replace with your database password

# JWT Configuration
JWT_SECRET=example_secret    # Replace with a strong secret key
JWT_EXPIRATION=1d            # Token expiration time (e.g., 1d, 12h, 1h)

# CORS Configuration
CORS_ORIGIN=http://localhost:3001 # Replace with your frontend domain or allowed origin

# Environment
NODE_ENV=development         # Options: development, production, test

# Optional Additional Configuration
LOG_LEVEL=info               # Logging level: error, warn, info, debug
